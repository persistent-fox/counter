export type TValues = 'startValue' | 'maxValue' | 'boardValue';

export type TValue = {
	id: string;
	title: string;
	value: number;
};

export type TDisabledBtn = {
	id: string;
	disabled: boolean;
};
